#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    float somme;
    printf("ecrit la valeur de A : ");
    scanf("%d", &a);

    printf("ecrit la valeur de B : ");
    scanf("%d", &b);

    if (a = b ){
     somme = (a+b)*3;
    printf("%f", somme);
    }


    return 0;
}
